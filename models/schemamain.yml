models:
  - name: cc_parcel_product
    columns:
      - name: parcel_id
        tests:
          - unique
          - not_null
  - name: cc_parcel
    columns:
      - name: parcel_id
        tests:
          - unique
          - not_null